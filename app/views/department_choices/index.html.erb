<h1>Listing department_choices</h1>

<table>
  <tr>
    <th>Student</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @department_choices.each do |department_choice| %>
  <tr>
    <td><%= department_choice.student_id %></td>
    <td><%= link_to 'Show', department_choice %></td>
    <td><%= link_to 'Edit', edit_department_choice_path(department_choice) %></td>
    <td><%= link_to 'Destroy', department_choice, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Department choice', new_department_choice_path %>
