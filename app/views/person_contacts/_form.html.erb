<%= form_for(@person_contact) do |f| %>
  <% if @person_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@person_contact.errors.count, "error") %> prohibited this person_contact from being saved:</h2>

      <ul>
      <% @person_contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.text_field :person_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
