<h1>Listing higer_education_backgrounds</h1>

<table>
  <tr>
    <th>Student</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @higer_education_backgrounds.each do |higer_education_background| %>
  <tr>
    <td><%= higer_education_background.student_id %></td>
    <td><%= link_to 'Show', higer_education_background %></td>
    <td><%= link_to 'Edit', edit_higer_education_background_path(higer_education_background) %></td>
    <td><%= link_to 'Destroy', higer_education_background, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Higer education background', new_higer_education_background_path %>
