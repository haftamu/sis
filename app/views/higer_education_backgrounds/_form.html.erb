<%= form_for(@higer_education_background) do |f| %>
  <% if @higer_education_background.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@higer_education_background.errors.count, "error") %> prohibited this higer_education_background from being saved:</h2>

      <ul>
      <% @higer_education_background.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br />
    <%= f.text_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
