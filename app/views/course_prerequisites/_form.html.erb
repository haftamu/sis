<%= form_for(@course_prerequisite) do |f| %>
  <% if @course_prerequisite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_prerequisite.errors.count, "error") %> prohibited this course_prerequisite from being saved:</h2>

      <ul>
      <% @course_prerequisite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.collection_select(:course_id,@courses,:id,:title_with_code) %>
  </div>
  <div class="field">
    <%= f.label :prerequisite_id %><br />
    <%= f.collection_select(:prerequisite_id,@courses,:id,:title_with_code) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
