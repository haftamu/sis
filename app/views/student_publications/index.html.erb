<h1>Listing student_publications</h1>

<table>
  <tr>
    <th>Student</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @student_publications.each do |student_publication| %>
  <tr>
    <td><%= student_publication.student_id %></td>
    <td><%= link_to 'Show', student_publication %></td>
    <td><%= link_to 'Edit', edit_student_publication_path(student_publication) %></td>
    <td><%= link_to 'Destroy', student_publication, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Student publication', new_student_publication_path %>
