<%= form_for(@student_qualification) do |f| %>
  <% if @student_qualification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_qualification.errors.count, "error") %> prohibited this student_qualification from being saved:</h2>

      <ul>
      <% @student_qualification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br />
    <%= f.text_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
