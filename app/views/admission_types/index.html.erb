<h1>Listing admission_types</h1>

<table>
  <tr>
    <th>Enrolment type</th>
    <th>Program type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admission_types.each do |admission_type| %>
  <tr>
    <td><%= admission_type.enrolment_type_id %></td>
    <td><%= admission_type.program_type_id %></td>
    <td><%= link_to 'Show', admission_type %></td>
    <td><%= link_to 'Edit', edit_admission_type_path(admission_type) %></td>
    <td><%= link_to 'Destroy', admission_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Admission type', new_admission_type_path %>
